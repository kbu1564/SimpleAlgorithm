#include <cstdio>
#include <iostream>
#include <algorithm>
using namespace std;
int main() {
	vector< pair<int, int> > vec;
	int N; scanf("%d", &N);
	for (int i = 0; i < N; i++) {
		int x, y; scanf("%d %d", &x, &y);
	}
	sort(vec.begin(), vec.end());
	for (int i = 0; i < N; i++) {
		for (int j = 0; j < N; j++) {
		}
	}
	return 0;
}
