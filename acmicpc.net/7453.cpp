#include <cstdio>
#include <iostream>
using namespace std;

int A[4001], B[4001], C[4001], D[4001];
int main() {
	int N; scanf("%d", &N);
	for (int i = 0; i < N; i++) {
		scanf("%d %d %d %d", &A[i], &B[i], &C[i], &D[i]);
	}
	return 0;
}
