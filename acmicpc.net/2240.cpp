#include <iostream>
using namespace std;

// DP[i][j][k] = i초때 자두가 j번 이동하여 k나무 아래에 있을때 받을 수 있는 자두의 개수
int DP[1001][31][3];
int main() {
	int T, W; cin >> T >> W;
	for (int i = 0; i < T; i++) {
		int drop; cin >> drop;
	}
	return 0;
}
