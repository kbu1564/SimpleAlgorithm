#include <iostream>
using namespace std;
int MAP[51][51];
int main() {
	int N, M;
	cin >> N >> M;
	for (int i = 0; i < N; i++) for (int j = 0; j < M; j++) cin >> MAP[i][j];
	for (int i = 0; i < N; i++) for (int j = 0; j < M; j++) {
		
	}
	return 0;
}