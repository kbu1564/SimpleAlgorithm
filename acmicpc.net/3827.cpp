#include <cstdio>
#include <iostream>
#include <algorithm>
using namespace std;
int main() {
	int N, M, A, B, C, T;
	while (true) {
		int S[51] = { 0, };
		cin >> N >> M >> A >> B >> C >> T;
		if (N + M + A + B + C + T == 0) break;

		for (int i = 0; i < N; i++) cin >> S[i];
	}
	return 0;
}
